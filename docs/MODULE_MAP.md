# Runtime Module Map

The wheel contains many Python modules because TardiSHA is a structured system, not one monolithic hash function. These are not separate versions.

| Module | Responsibility |
|---|---|
| `alqc_digest.py` | Twelve-lane sponge, absorption, and squeeze behavior |
| `source_emission.py` | Final Equation Z source emission, Parliament bodies, Golden bearings, Truth closure |
| `hashing.py` | Canonical values, files, directories, middle streams, and source domains |
| `route.py` | Complete source route and ordered Goetic pair |
| `canon.py` | Governing constants, Goetic registry interface, and route restrictions |
| `court_registry.py` | The 12 by 12 ordered Court body and reciprocal addresses |
| `aeon_layers.py` | Immutable Goetics, Court breath, and Domus breath witnesses |
| `domus.py` | Living Domus resolution, visible grammar, parser, and verifier |
| `domus_stream.py` | Bounded-memory file and directory Domus writing and verification |
| `mirror_math.py` | Exact Aeternum terminal return at the Grimchain depth actually chosen by the user |
| `manifestation.py` | Downstream finite-return D-COMP and manifestation witnesses |
| `trig.py` | TRIG Mirror witness and return obligations |
| `tripartite.py` | Locus, Shadow Locus, and Axiomyr witness structure |
| `phase_evolution.py` | Source-bound phase evolution and ordered witness cycle |
| `verification_appendices.py` | Canon verification appendix structures |
| `qstate_glyphs.py` | Fixed Q-state positions and visible Q-glyph mapping |
| `personality_traits.py` | Court personality witness registry |
| `seal.py` | Ordinary finite seals and streaming seal verification |
| `node.py` | Serializable source-bound TardiSHA nodes |
| `archive.py` | Reversible chunk archive, manifest, and restore path |
| `manifest.py` | File, directory, and recursive `.grim` bodies with the terminal `THIS FILE` self-return |
| `folding.py` | Prefix-stable folding and ladder witnesses |
| `stream.py` | Packet and code-point streaming helpers |
| `living_alphabet.py` | Canonical Synodic Magicae alphabet |
| `grimchain.py` | Primary command-line interface |
| `cli.py` | Package command entry point |
| `__init__.py` | Public Python API |
| `__main__.py` | `python -m TardiSHA` entry point |

The wheel installs this same package and generates the `grimchain`, `tardisha`, and `TardiSHA` commands.

## Exact and compiled bodies added in 23.0.3

- `TardiSHA/manifestation.py`: exact ℚ(√5) Ennead ledger, integer Q-vector metric squares, exact D-COMP decision body, and `C_bio² = 61009/44`.
- `TardiSHA/form_cache.py`: opt-in Q₀ Form-signature cache, atomic JSON commit, and exact reconstruction of cached emissions and route witnesses.
- `TardiSHA/_alqc_kernel.c`: required compiled implementation of absorption and the 144-byte round cadence.
- `TardiSHA/manifest.py`: ordered process-pool file emission and cache join.

`mirror_math.py` remains the audited exact Aeternum return body and was not altered by this remediation.
