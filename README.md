# TardiSHA
## Time and Relative Dimension in Space Hash

**The first public ALQC software implementation.**

**A deterministic structured-content seal whose path out carries its path back.**

> In the black hour the old star opens.  
> A name walks barefoot through the ash.  
> Five marks wake upon the threshold,  
> and the road remembers the hand that sent it home.

TardiSHA is an ALQC-native source engine, Final Equation 𝔃 route, Court and Domus derivation, Synodic Magicae manifestation, and Aeternum Mirror operator implemented for ordinary computers.

It accepts files, directory trees, standard input, and canonical in-memory values. It produces a source-bound public GrimChain consisting of the manifested middle only: `⛎⛎⛎` at depth zero, `☽᳀☾` at depth one, or exactly `n` generated Synodic Magicae coordinates at every depth `n > 1`, with no inserted inner Axiomyr. The source-resolved Goetics, Courts, complete Domus, Q-body, TRIG, Tripartite, phase, and verification witnesses remain inside the derivation that gives the public GrimChain its identity.

The GrimChain at any user-chosen middle depth can be appended as its own raw UTF-8 line to the file that produced it. Nothing in ordinary GrimChaining searches for, strips, or interprets an earlier chain: the resulting physical bytes become the next source body, and the next witness is calculated from that whole body exactly as it stands.

```bash
grimchain source.txt
```

## The Record Organizes Itself

A GrimChain can preserve the character of the whole without repeating local phrases. In one fixed 20,736-coordinate witness generated **once** from the exact string `I am the Record of the Lattice?`, the same unchanged body was read three separate times. The record did not mutate between readings. What changed was the relational structure made visible by another pass through the same body.

The first reading exposed return: 189 distinct generated symbols recurring across long distances. The second exposed relation: 15,718 distinct directed adjacent transitions, with 6,958 of those relation kinds also appearing in the reverse direction somewhere else in the same record. The third exposed distributed persistence: no exact four-glyph or eight-glyph window repeated, while the two halves retained a composition similarity of `0.984045` and quarter-to-quarter comparisons remained between `0.960483` and `0.971563`.

Local speech keeps becoming new while the large body keeps becoming itself.

That is the self-organizing character of the written GrimChain: **local novelty under global invariance**. Identity is not forced by copying a phrase back into place. It is distributed through recurrence, relation, return, and the conservation of the whole. The reader may reorganize its patterning around the same fixed record; the GrimChain does not need to change in order to reveal another lawful relation already present within it.

> No sentence returns, yet the kingdom returns.  
> No four marks need remember their order.  
> The field keeps faith without copying its face,  
> and every road learns the shape of home.

The poetry moves as the mathematics moves: not by explaining the mechanism twice, but by carrying its conservation through another office. The utterance changes register while the identity remains one.

## One exact depth-zero GrimChain

For the exact UTF-8 source bytes `The path out carries its path back.\n`, TardiSHA produces:

```text
⛎⛎⛎
```

The public depth-zero GrimChain is exactly the Triple Goddess `⛎⛎⛎`, manifested from the Shadow Locus `⛎`.

At depth one, the public GrimChain is exactly the Triple Horned God `☽᳀☾`, with Axiomyr `᳀` at its center.

At every depth `n > 1`, the public GrimChain contains exactly `n` generated Synodic coordinates. No inner Axiomyr is inserted into that coordinate body. The fixed Axiomyr projection belongs to depth one alone; the complete Axiomyr and Tripartite offices remain inside the derivation rather than being written into every positive public manifestation.

Verification independently recomputes the source emission, Goetics, Court C, Court D, Domus identity, requested manifested middle, and public GrimChain, and accepts the carried body only when it returns exactly from its source.

> Iron sleeps beneath the river.  
> A pale lantern turns in the reeds.  
> The hinge waits where no foot may linger,  
> and dawn returns wearing another face.

For a complete positional translation guide to the native glyph body, the twelve Goetics, and all 144 Courts, see [`DICTIONARY.md`](docs/DICTIONARY.md).

## The Axiomyr Hinge

The depth-zero GrimChain closes through the Triple Goddess `⛎⛎⛎`, manifested from the Shadow Locus `⛎`: the compressed public condition in which no finite Synodic coordinate body has been chosen to emanate.

Depth one is the singular public Axiomyr projection: exactly `☽᳀☾`. Here `᳀` is visibly the BoundaryWalker condition, the Hinge between the first and the last, holding the Center of Breath between Yes and No, Light and Shadow, real and imaginary.

At every depth greater than one, the Hinge governs without being inserted into the public coordinate field. The written body is exactly the requested number of generated Synodic coordinates. Axiomyr remains a complete internal Tripartite office of the resolved universe, while the public manifestation keeps only the coordinates it was asked to speak.

The public projection law is exact:

- depth `≤ 0` → exactly `⛎⛎⛎`;
- depth `1` → exactly `☽᳀☾`;
- depth `n > 1` → exactly `n` generated Synodic coordinates, with no inserted inner Axiomyr.

> At the still door no crown is spoken.  
> Night parts its hair around a single flame.  
> The road leaves the hinge behind it,  
> yet every turning remembers where it crossed.

---

## The completed source-to-Domus path

The implemented order is strict:

```text
finite source body F
    ↓
ALQC sponge with twelve finalized 64-bit Goetic lanes
    ↓
structural measure S and AHN-relative operational measure O
    ↓
𝔃₀ through Φ⁻¹ and 𝔃₁ through Φ⁻²
    ↓
ordered Goetic pair (gᵢ,gⱼ)
    ↓
governing Court C
    ↓
144-entry Court product measure Wᴿ = O ⊗ S
    ↓
independently resolved alternating Court D
    ↓
exact ξ_D within D's own interval
    ↓
ν_H = Ω_D + ξ_DΦ²
    ↓
Q-body, manifestation, TRIG, Tripartite separation, C09 phase-evolution, and C11 verification witnesses
    ↓
one Domus identity
    ↓
`⛎⛎⛎`, `☽᳀☾`, or exactly the requested finite Synodic coordinate body
    ↓
Aeternum Mirror return
```

Each layer preserves the offices already established before it and carries them forward in order.

---

## 1. One source emission, twelve Goetic lanes

For a source `F`, the completed ALQC sponge emits twelve unsigned 64-bit lanes:

`E(F) = (L₀,L₁,…,L₁₁)`

Every lane satisfies `0 ≤ Lₖ < 2⁶⁴`.

All twelve finalized lanes participate in resolving the Goetic pair through Parliament and the two Golden bearings.

The complete `SourceEmission` carries the 256-bit public source digest together with the twelve lanes, structural and operational measures, both Fraktur cadences, source size, source domain, and exact closure witness required by the Final Equation `𝔃` route.

The supported source domains are canonical material, raw files, and directory trees. Filesystem metadata is excluded from raw-file identity. Directory identity is constructed from the canonical tree record.

---

## 2. Structural and operational Parliament bodies

The same finalized source state is read through two conserved measures.

The structural measure is the Parliament-ordered lane body:

`Sₖ = Lη(pₖ)`

Its total is strictly positive:

`ΣS > 0`

The operational measure begins at AHN, the exact Parliament phase seam. Directed gaps around one complete 64-bit turn form:

`Oₖ = Δη(pₖ)`

and satisfy:

`ΣO = 2⁶⁴`

Structure and operation are two conserved readings of the same completed emission, joined through one source identity.

Parliament order is preserved under both bearings. Mirror Math changes bearing while the procession remains ordered.

---

## 3. Final Equation 𝔃

The completed state is squeezed into two consecutive 256-bit cadences:

`σ(E(F)) = 𝔃₀(F) ‖ 𝔃₁(F) ‖ …`

The structural bearing is:

`ALPHA = Φ⁻¹ = (√5 − 1)/2`

The operational bearing is:

`BETA = Φ⁻² = (3 − √5)/2`

The implementation resolves both intervals in exact `ℚ(√5)` arithmetic through the exact Golden bodies `Φ⁻¹` and `Φ⁻²`.

The exact Golden closure is:

`Φ⁻¹ + Φ⁻² = 1`

The source-route witness calculates independent rational and irrational residual coefficients. It derives:

`D-COMP(𝔃) = 0`

`Truth(𝔃) = 1`

These values arise from the completed measures and Golden identities as the exact closure of the route.

---

## 4. The ordered Goetics and governing Court C

The two Parliament bearings resolve the ordered Goetic pair:

`gᵢ = Resolve₁₂(S, phase=𝔃₀, bearing=Φ⁻¹)`

`gⱼ = Resolve₁₂(O, phase=𝔃₁, bearing=Φ⁻²)`

Only after both Goetics resolve does governing Court C exist:

`C = Cᵢⱼ`

`address(C) = 12i + j`

There are exactly twelve governing roots and twelve alternating roots, therefore exactly:

`12² = 144 ordered Courts`

The twelve diagonal self-Courts remain lawful as exact ordered Court identities.

### Court inheritance is partitioned

Court C receives identity, Q-bias, and Q-vector from its governing Goetic `gᵢ`.

Court C receives its resonance anchor from its alternating Goetic `gⱼ`:

`Ω_C = frequency(gⱼ)`

The pure Goetic frequency remains immutable. Court breath is a source-bound displacement around that anchor:

`ν_C = Ω_C + δ_C`

`|δ_C| ≤ Φ`

A Court carries a bounded operational breath around the alternating Goetic's immutable pure frequency.

The Final Equation route also records the transposed reciprocal address `Cⱼᵢ` as its own route witness. Court D is independently resolved from the 144-entry Court product measure and enters Domus motion through that office.

---

## 5. Alternating Court D is independently resolved

The Domus consumes Courts as its atoms, and its second Court is independently resolved as Court D.

From the already completed source emission, TardiSHA constructs the 144-entry Court product measure in canonical row-major Court order:

`Wᴿ[k,l] = O[k]S[l]`

`address = 12k + l`

Its total closes exactly:

`ΣWᴿ = (ΣO)(ΣS) > 0`

After normalization:

`Σ(Wᴿ/ΣWᴿ) = 1`

Court D is then resolved across all 144 Court intervals:

`D = Resolve₁₄₄(Wᴿ, phase=𝔃₁, bearing=Φ⁻²)`

D resolves from the completed source emission through `𝔃₁` and the canonical 144-entry Court product measure.

The implementation retains all 144 exact product weights, D's selected interval, the effective Golden point, cumulative bounds, totals, and the exact local coordinate.

---

## 6. Exact Domus motion

The signed position of the `𝔃₁` bearing inside D's own interval is:

`ξ_D = 2(effective_bearing − cumulative_before)/(cumulative_after − cumulative_before) − 1`

The carried exact witness proves:

`−1 ≤ ξ_D ≤ 1`

The Domus anchor is the pure frequency of D's alternating Goetic:

`Ω_D = frequency(D.alternating_goetic)`

It is the pure Goetic anchor carried by Court D; Domus motion breathes from that anchor through `ξ_DΦ²`.

The canonical Domus resonance is:

`ν_H = Ω_D + ξ_DΦ²`

Therefore:

`|ν_H − Ω_D| = |ξ_D|Φ² ≤ Φ²`

The bound closes exactly in `ℚ(√5)` before its runtime image is formed.

The Domus frequency derivation begins from Court D's carried Goetic anchor and the exact local coordinate `ξ_D`, yielding `ν_H = Ω_D + ξ_DΦ²`.

The layer law is therefore preserved:

`Goetic → Court: bounded by Φ`

`Court → Domus: bounded by Φ²`

Courts move through Goetics. Domus moves through Courts.

---

## 7. The exact C×D crossing body

C and D each range over the complete 144-Court body. The ordered crossing index is:

`index(C,D) = 144·address(C) + address(D)`

Its cardinality is exactly:

`|C×D| = 144² = 20,736`

The indices run from `0` through `20,735` without collision.

This is the finite ordered Court-crossing body. Each crossing carries multiple finite depths, manifestations, source identities, nonces, and return-lineage positions.

The Liquid connection governor remains separate:

`110 active + 34 withheld = 144 total`

The Liquid threshold body is the typed governor `⟨110|144|34⟩`: 110 active positions, 34 withheld positions, 144 total. The withheld body preserves contour and prevents whiteout.

---

## 8. The inherited Q-body and written GrimChain

The Domus still inherits its Q-body from governing Court C through C's governing Goetic. The four canonical Q-state positions remain:

`Q₀ → 🜔   Form / latent structural presence`

`Q₁ → 🜕   Truth / archive / rational commitment`

`Q₂ → 🜖   Shadow / debt / pressure to be converted`

`Q₃ → 🜗   Recursion / residue / return-fire`

Those Q-state bodies remain internal witnesses of the resolved Domus, carried through the derivation that manifests the public GrimChain.

The public GrimChain is the manifested middle only.

At depth zero, the public body is exactly `⛎⛎⛎`. At depth one it is exactly `☽᳀☾`.

At depth greater than one, the same Domus identity unfolds through the generated Synodic Magicae coordinate alphabet of **189 glyphs**. Across all public projection states, the renderer must cover **191 distinct Unicode code points**: the generated alphabet together with the fixed public Shadow-Locus and Axiomyr offices. The complete internal Domus, Breath, Prosody, Crown, Court, Q-body, TRIG, and Tripartite witnesses remain in their own offices while the public coordinate stream retains only its written coordinates.

The stream is resolved against deterministic 4,096-coordinate epoch boundaries, but only the requested extent is calculated. Depth changes the written manifestation without rerouting the source or creating another root. For every depth greater than one, the public body is exactly the requested coordinate extent and contains no inserted inner Axiomyr.

## The public GrimChain state-space

The public GrimChain carries only the manifested middle. The source-derived Goetics and Courts remain internal witnesses of the completed source route and are not written around the public body.

### Internal witness widths

The implementation carries several exact internal bodies:

| Internal body | Exact width or cardinality | Plain meaning |
|---|---:|---|
| C09 folded-state witness | `2¹²⁶` | The exact `folded_states` body carried by the C09 Golden-ratio witness |
| One source digest | `2²⁵⁶` | One 256-bit source-coordinate body |
| One Fraktur cadence | `2²⁵⁶` | One `𝔃₀` or `𝔃₁` cadence body |
| Ordered cadence pair | `2⁵¹²` | The combined type of `(𝔃₀,𝔃₁)` |
| Twelve 64-bit lanes | `2⁷⁶⁸` | The complete twelve-lane binary container |
| One Domus identity | `2²⁵⁶` | One domain-separated Domus commitment body |

These bodies remain bound to the same source-rooted derivation while occupying different internal offices.

### The written middle

Let `Gₙ(F)` be the public GrimChain for source `F` at declared depth `n`.

Its written extent is the projection law itself:

`|Gₙ| = 3` for `n ≤ 0`

`|G₁| = 3`

`|Gₙ| = n` for every `n > 1`.

Depth zero has one fixed public body, `⛎⛎⛎`. Depth one has one fixed public body, `☽᳀☾`. At every depth `n > 1`, the public body contains exactly `n` coordinates drawn from the 189-glyph generated alphabet, so the abstract coordinate state-space at that declared depth contains `189ⁿ` possible coordinate strings. For one fixed source, source domain, nonce, and Domus identity, TardiSHA deterministically resolves exactly one of them.

At depth 64, the public GrimChain therefore contains exactly 64 generated Unicode coordinates. At depth 4,096 it contains exactly 4,096. The internal deterministic epoch boundary does not impose a public output cap and does not add a center glyph.

### Holographic persistence

The Domus is a persistent relational identity whose source route, Courts, Q-body, frequencies, witnesses, and return law are established before the middle is manifested. The equations are its anatomy. The GrimChain is its finite written utterance.

At depth greater than one, the coordinate body is reproduced deterministically from the resolved Domus identity. Increasing depth preserves the same source root, Court lineage, nonce, and return law while revealing another finite extent of the same source-bound coordinate manifestation.

Every non-negative finite declared depth is lawful. Each individual GrimChain remains finite, while the sequence of finite depths continues without terminal bound.

> A black seed sleeps beneath seven winters.  
> Spring finds it without asking its name.  
> One green tongue breaks the earth,  
> and the buried kingdom remembers rain.

## The Aeternum Mirror

The Aeternum Mirror belongs to the mathematical route, not to a hidden parser searching files for an earlier GrimChain.

For the completed source route `R`, Mirror preserves operator position and procession while exchanging bearing:

`Manifest ↔ Reflect`

`Ascend ↔ Descend`

`Utter ↔ Receive`

`+ ↔ −`

Self-bearing acts remain in their positions. The reading order is not reversed. The completed relation closes as an involution:

`Mirror(Mirror(R)) = R`

with exact Mirror closure:

`D-COMP_Mirror = 0`

and the route remains in the same operator order.

Let `Gₙ(F)` be the exact public GrimChain derived from physical source body `F` at declared depth `n`. A raw self-embedding simply appends that UTF-8 GrimChain as its own final line:

`F′ = F ‖ newline_if_needed ‖ UTF8(Gₙ(F))`

After that write, `F′` is a new physical source body. Ordinary GrimChaining does not search for `Gₙ(F)`, does not strip it, does not interpret it specially, and does not attempt to recover `F`. It witnesses the bytes that are present:

`Gₙ(F′)`

This is the preservation law of pure speech. The earlier utterance remains matter inside the later source. Rechaining is therefore a new witness of the complete present body, not a rollback to a previous one.

The explicit PDF return office is separate. It can embed, replace, verify, and remove its own exact PDF return while ordinary PDF GrimChaining continues to witness the current physical PDF bytes exactly like every other file.

> The road does not erase its footprints.  
> Ash becomes earth beneath the next footfall.  
> The mirror keeps the order of the walking,  
> and what was spoken becomes part of what can speak.

---

## Canon boundary in the runtime

The Locus of Invariability `♾` remains the non-traverse boundary of the runtime. The Tripartite witness carries that boundary while source, route, Court, Q-state, digest, and visible Grimchain remain within the traversable body.

At depth zero, the visible middle is exactly the Triple Goddess `⛎⛎⛎`, manifested from the Shadow Locus `⛎`; Supervenience `⟠` retains its typed witness office elsewhere in the derivation.

`⟠` carries the typed Supervenience witness of the rooted Domus derivation. `⛎⛎⛎` occupies the depth-zero public middle, `☽᳀☾` is the exact depth-one public Axiomyr projection, and every depth greater than one carries only its requested generated Synodic coordinates.

The 110-active, 34-withheld Liquid body remains the typed `⟨110|144|34⟩` governor throughout the runtime.

---

## Exact mathematics and the compiled kernel

TardiSHA 23.2.3 carries the decision bodies of the Canon as exact computational bodies:

- `PHI` and `PHI_SQUARED` are exact elements of ℚ(√5). `PHI_IMAGE` and `PHI_SQUARED_IMAGE` are their explicit runtime images; the exact algebraic bodies remain authoritative.
- The nine-strike Ennead ledger is conserved in exact ℚ(√5). The Q₃ transfer and accumulated Form work close by coefficient equality.
- `C_bio` is identified by the exact square `61009/44`; that exact square carries the identity gate.
- Q-vectors, metric squares, the 110 active / 34 withheld / 144 total governor, and D-COMP closure remain exact integer or rational bodies.
- Aeternum Mirror Math closes at exact D-COMP zero while preserving operator order and exchanging bearing; exact byte-residual comparison remains a separate verification office where byte identity is explicitly required.

### Exact bodies and witness images

TardiSHA carries exact mathematics and its runtime images as different offices of the same derivation. The exact body governs; the image witnesses.

`Fraction`, exact integers, `GoldenBearing`, `Q5Fraction`, exact squared bodies, exact Court measures, exact interval bounds, and exact byte equality determine routing, conservation, admission, closure, D-COMP, and Truth. Float and complex values expose the corresponding frequency, motion, compatibility, and serialized witness surfaces after the exact body has been established.

The exact body governs and the runtime image witnesses. `ExactEnneadLedger` governs Ennead conservation while `EnneadResult` exposes its runtime image. `ExactDComp` governs Mirror closure while `DCompWitness` exposes the residual, debt, work, and recursion image surface. Court and Domus frequency images arise from exact phase, bearing, and ℚ(√5) displacement bodies after their bounds close. Tripartite and TRIG reconstruct the exact Court-rooted frequency body and return the supplied complex image from that exact body.

Exact mathematics produces the runtime image.

The per-byte sponge is carried by the compiled extension `TardiSHA._alqc_kernel`. The wheel and direct Debian package carry the compiled kernel; the Launchpad source package builds it from `TardiSHA/_alqc_kernel.c`.

Recursive manifests distribute independent file emissions across a process pool while preserving sorted manifest order. Every manifest run reads and witnesses every selected source byte.

## Install

Python 3.10 or newer is required by the Python package. The compiled `TardiSHA._alqc_kernel` carries the sponge, and the PDF return office uses PyMuPDF.

### Ubuntu PPA handoff

The Launchpad source package is prepared under `installers/python_build/linux_ubuntu/launchpad/`. When the current release is published in `ppa:witchofalways/ppa`, install it with:

```bash
sudo add-apt-repository ppa:witchofalways/ppa
sudo apt update
sudo apt install tardisha
```

### Direct Debian package

```bash
sudo apt install ./installers/python_build/linux_ubuntu/dist/tardisha_23.2.3-1_amd64.deb
```

### Wheel

```bash
python3 -m pip install installers/python_build/linux_ubuntu/dist/tardisha-23.2.3-*.whl
```

### Source distribution

```bash
python3 -m pip install installers/python_build/linux_ubuntu/dist/tardisha-23.2.3.tar.gz
```

Installed command names are:

```text
grimchain   primary command
tardisha    package command
TardiSHA    compatibility spelling
```

The wheel contains the complete `TardiSHA` package and the compiled `_alqc_kernel` extension.

---

## Commands

Basic help:

```bash
grimchain --help
```

Complete help:

```bash
grimchain --help -a
```

### Seal a file or directory

```bash
grimchain path/to/file
grimchain path/to/directory
```

### Choose Synodic Magicae depth

```bash
grimchain 64 path/to/file
grimchain --middle 64 path/to/file
```

Leaving the number out selects the compressed Shadow Locus `⛎` center. A supplied non-negative whole number declares the middle depth; `64` is one example.

### Read standard input

```bash
printf 'memory returns through crystal' | grimchain
```

### Write and verify a streamed Grimchain

```bash
grimchain 1000000 source.bin --output source.grim
grimchain source.bin --verify --output source.grim
```

### Grimchain an exact quoted string

```bash
grimchain --string "hello world"
grimchain 64 --string "hello world"
```

`--string` receives the exact UTF-8 content of one shell argument. The shell removes the surrounding quotes before Grimchain receives it. Omitting the number uses the compact Shadow Locus `⛎` center; supplying a positive number follows the invariant Axiomyr center law: even depth preserves that many generated coordinates around `᳀`, while odd depth reserves the center for `᳀` and generates one fewer surrounding coordinate. The same middle, nonce, output, and verification laws govern every source form.

Shell quoting belongs to the shell. Single quotes preserve characters such as `$HOME` literally; double quotes allow shell expansion before Grimchain receives the resulting string.

### Create manifests

```bash
grimchain 64 --manifest file.bin
grimchain 64 --manifest directory
grimchain 64 -R --manifest directory
grimchain 64 --manifest directory --output chosen-name.grim
```

The command first writes the complete manifest body and the waiting `THIS FILE` line. It then Grimchains that prepared `.grim` using the exact middle selected by the user, appends only that Grimchain and one terminal newline, Grimchains the completed `.grim` again at the same middle, and prints the final result. The stored `THIS FILE` value and the terminal output are byte-identical.

Omitting `NUMBER` uses the compact Shadow Locus `⛎` center throughout. Supplying positive `NUMBER` carries the same invariant Axiomyr center law through every manifest entry, the manifest-body Grimchain, the first `THIS FILE` pass, and the final confirming pass.

The physical ending is:

```text
THIS FILE
🜛...the exact user-depth Grimchain...🜛
```

The `THIS FILE` header exists before the first self-chain. After that first pass, only the Grimchain itself and its final newline are appended.

### Manifest source truth

Every manifest pass reads and witnesses every selected source byte as present source truth.

### Verify a manifest

```bash
grimchain 64 --manifest directory --verify --output chosen-name.grim
```

### Inspect a visible Grimchain

```bash
grimchain '🜛...🜛' --inspect
```

Inspection parses the visible grammar; verification recomputes that grammar from the source.

### Emit a fold ladder

```bash
grimchain source.bin --fold --span 100000 --levels 8
```

### Select a deterministic nonce

```bash
grimchain source.bin --nonce 42
```

The nonce selects another repeatable source-bound manifestation from the same source law.

### Append and remove a PDF return

```bash
grimchain 64 --pdf-embed document.pdf
grimchain --pdf-rm-embed document.pdf
```

The PDF return writes the complete GrimChain as Unicode text through the PDF text-layout engine. The installed operating-system font body supplies the written glyphs. The return remains incremental, source-bound, nonce-bound, and retracts to the exact original PDF bytes after verification.

### Exact file bytes

File inputs enter as exact bytes, preserving their written byte body.

### Multiple sources

```bash
grimchain path1 path2 directory
```

---

## Python API

### Read the complete source route

```python
from TardiSHA import file_emission, source_route_witness_from_emission

emission = file_emission("sample.bin")
route = source_route_witness_from_emission(emission)

print(route.origin_glyph, route.resolution_glyph)
print(route.court_address, route.reciprocal_address)
print(route.route_dcomp, route.truth)
```

The route's reciprocal address is the Final Equation transpose witness; Domus Court D is independently resolved through the Court product measure.

### Resolve Court D and its exact bearing

```python
from TardiSHA import file_emission, resolve_court_bearing

emission = file_emission("sample.bin")
bearing = resolve_court_bearing(emission)

print(bearing.court.address, bearing.court.glyph)
print(bearing.xi)
print(bearing.phi_squared_displacement)
assert bearing.product_measure_verifies
assert bearing.exact_bound_verifies
```

### Generate a GrimChain value

```python
from pathlib import Path
from TardiSHA.domus_stream import living_domus_for_source

value = living_domus_for_source(Path("sample.bin"), 64, kind="file")
print(value)
```

### Inspect the Aeternum Mirror derivation

```python
from TardiSHA import file_emission, derive_goetics, verify_derivation

emission = file_emission("sample.bin")
derivation = derive_goetics(emission)

print(derivation.same_operator_order)
print(derivation.route_dcomp, derivation.truth)
assert verify_derivation(derivation)
```

### Reversible archive

```python
from TardiSHA import create_archive, restore_archive

manifest = create_archive("sample.bin", "archive-store")
restore_archive(
    "archive-store/manifest.json",
    "archive-store",
    "restored/sample.bin",
)
```

Archives preserve physical bytes exactly, including any raw GrimChain line already present in the source. Restore preserves the archived source basename, so `sample.bin` returns as `sample.bin` beneath the chosen restoration directory. No hidden Mirror retraction rewrites the stored byte ledger.

Practical API details remain in [`docs/PYTHON_API.md`](docs/PYTHON_API.md). Module responsibilities remain in [`docs/MODULE_MAP.md`](docs/MODULE_MAP.md).

---

## Repository map

```text
TardiSHA/                  runtime package
grimchain                  primary source-tree launcher
COMMANDS.md                complete command reference
CONTRIBUTING.md            contribution guide
ORIGIN.md                  origin and authorship record
docs/                      mathematics, implementation, API, glossary, and proof documentation
examples/                  executable examples
fonts/                     installer source font resources
installers/                platform installer construction bodies
LICENSE                    Mozilla Public License 2.0
COPYRIGHT                  copyright notice
CITATION.cff               citation metadata
VERSION                    public version number
```

---

## What TardiSHA is

TardiSHA is:

- a deterministic ALQC source emission;
- a twelve-lane state machine;
- exact Golden Parliament routing;
- an ordered 144-Court body;
- an exact 20,736-position C×D crossing body;
- a Court-rooted Domus resonance under `ν_H = Ω_D + ξ_DΦ²`;
- a source-bound public GrimChain consisting of the manifested middle only;
- a deterministic, demand-rendered Synodic Magicae manifestation whose public law is `⛎⛎⛎` at depth zero, `☽᳀☾` at depth one, and exactly `n` generated coordinates at every depth `n > 1`;
- an Aeternum Mirror route that preserves operator order, exchanges bearing, and closes at exact D-COMP zero;
- source, file, directory, node, stream, manifest, and reversible archive tooling;
- an exactly countable discrete Domus state-space across unbounded finite depth.


Its strength lies in keeping each body in its proper office, carrying the exact lineage, and proving the return.

---

## License

TardiSHA source code and software documentation are licensed under the **Mozilla Public License 2.0** (`MPL-2.0`). See [`LICENSE`](LICENSE).

Copyright © 2026 Magus Jamye Reficul Ahnend.

Non-code works carrying a separate notice retain that notice.

---

## Closing seal

> Bone remembers beneath winter soil.  
> Ash lifts from the sleeping hearth.  
> A traveler crosses the moonlit field,  
> finds the old door open,  
> and enters wearing the same shadow.

`Mirror(Mirror(R)) = R`

`D-COMP_Mirror = 0`

`Truth = 1`
